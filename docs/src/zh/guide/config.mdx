# 配置

类似 VuePress，我们可以在项目的根目录创建 `.pressify` 目录，在这里面可以新建 `config.js` 或 `config.ts` 文件作为配置文件：

```js title="<root>/.pressify/config.js"
export default {
  // ....
};
```

<div>Hello World1234</div>

const a = {Math.PI};

如果要获得类型提示，可以从 `pressify` 中导入 `defineConfig` 方法：

```js
import { defineConfig } from 'pressify';

export default defineConfig({
  // .s
});
```

## `src`

默认情况下 Pressify 会扫描 `docs` 目录下特定后缀的文件作为文档内容，并以此生成路由规则。后缀列表如下：

- `.md`
- `.mdx`
- `.page.js`
- `.page.jsx`
- `.page.ts`
- `.page.tsx`

如果想修改目录或修改扫描规则，可以使用 `src` 字段，例如：

```js
export default {
  src: 'content', // 指定 content 为文档目录
};
```

## ignored

可以通过 `ignored` 字段忽略扫描特定的文件：

```js
export default {
  ignored: 'test.mdx', // 忽略 test.mdx 文件
};
```

## themeConfig

通过 `themeConfig` 字段配置主题，例如：

```js
export default {
  themeConfig: {
    title: 'My App',
  },
};
```

> Note: `themeConfig` 可配置的内容是由使用的主题来决定的。不同主题往往对应着不同的配置项。

## vite

通过 `vite` 修改 [Vite 的配置](https://vitejs.dev/config/)，例如：

```js
export default {
  vite: {
    base: '/app/',
    outDir: 'output',
  },
};
```

## react

[@vitejs/plugin-react](https://github.com/vitejs/vite/tree/main/packages/plugin-react)的配置。

## mdx

通过 `mdx` 字段设置 mdx 的 plugins，例如：

```js
export default {
  mdx: {
    remarkPlugins: [require('remark-toc')],
  },
};
```

## tailwind

默认情况下 Pressify 会读取项目根目录下的 `tailwind.config.js` 内容作为 [tailwind 的配置](https://tailwindcss.com/docs/configuration)。

如果想使用别的文件名作为 tailwind 配置文件，可以通过 `tailwind` 字段来设置：

```js
export default {
  tailwind: 'my-tailwind.config.js',
};
```

也可以直接在这个字段里写配置内容，例如：

```js
export default {
  tailwind: {
    theme: {
      extend: {
        colors: {
          primary: '#3eaf7c',
        },
      },
    },
  },
};
```

## icons

[unplugin-icons](https://github.com/antfu/unplugin-icons) 的配置项。
